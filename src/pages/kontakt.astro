---
import Layout from '../layouts/Layout.astro';
import site from '../data/site.json';
import standorte from '../data/standorte.json';

const woerth = standorte.find((s: any) => s.id === 'woerth');
---

<Layout title="Kontakt – Praxis Med Vital" description="Nehmen Sie Kontakt auf und vereinbaren Sie Ihren Termin bei Praxis Med Vital.">
  <section class="page-header">
    <div class="container">
      <span class="eyebrow">Kontakt</span>
      <h1>Wir sind für Sie da</h1>
      <p>Rufen Sie an, schreiben Sie uns oder nutzen Sie das Kontaktformular.</p>
    </div>
  </section>

  <section class="sec reveal">
    <div class="container">
      <div class="contact-grid">
        <div>
          <span class="eyebrow">Erreichbarkeit</span>
          <hr class="rule"/>
          <h2 class="section-title" style="margin-top:18px">Kontaktdaten</h2>

          <div style="margin-top:28px">
            <div class="contact-detail">
              <div class="contact-detail__icon">A</div>
              <div class="contact-detail__text">
                <h4>Adresse</h4>
                <p>{woerth?.address}<br/>{woerth?.zip} {woerth?.city}</p>
              </div>
            </div>
            <div class="contact-detail">
              <div class="contact-detail__icon">T</div>
              <div class="contact-detail__text">
                <h4>Telefon</h4>
                <p><a href={`tel:${site.phonePlain}`}>{site.phone}</a></p>
              </div>
            </div>
            <div class="contact-detail">
              <div class="contact-detail__icon">@</div>
              <div class="contact-detail__text">
                <h4>E-Mail</h4>
                <p><a href={`mailto:${site.email}`}>{site.email}</a></p>
              </div>
            </div>
            <div class="contact-detail">
              <div class="contact-detail__icon">F</div>
              <div class="contact-detail__text">
                <h4>Fax</h4>
                <p>{site.fax}</p>
              </div>
            </div>
          </div>

          <h3 style="font-size:18px;margin-top:32px;margin-bottom:4px">Öffnungszeiten</h3>
          <table class="hours-table">
            {site.hours.map((h: any) => (
              <tr>
                <td>{h.days}</td>
                <td>{h.time}</td>
              </tr>
            ))}
          </table>
        </div>

        <div>
          <span class="eyebrow">Nachricht</span>
          <hr class="rule"/>
          <h2 class="section-title" style="margin-top:18px">Termin anfragen</h2>
          <p style="font-size:14px;font-weight:300;color:var(--ink-mid);margin:14px 0 24px">
            Füllen Sie das Formular aus und wir melden uns schnellstmöglich.
          </p>
          <form class="contact-form" name="kontakt" method="POST" data-netlify="true" netlify-honeypot="bot-field" style="max-width:100%">
            <input type="hidden" name="form-name" value="kontakt" />
            <p class="full" style="display:none">
              <label>Nicht ausfüllen: <input name="bot-field" /></label>
            </p>
            <div class="form-group">
              <label for="vorname">Vorname *</label>
              <input type="text" id="vorname" name="vorname" required />
            </div>
            <div class="form-group">
              <label for="nachname">Nachname *</label>
              <input type="text" id="nachname" name="nachname" required />
            </div>
            <div class="form-group full">
              <label for="email">E-Mail *</label>
              <input type="email" id="email" name="email" required />
            </div>
            <div class="form-group full">
              <label for="telefon">Telefon</label>
              <input type="tel" id="telefon" name="telefon" />
            </div>
            <div class="form-group full">
              <label for="betreff">Betreff</label>
              <select id="betreff" name="betreff">
                <option value="">Bitte wählen</option>
                <option value="termin">Terminanfrage</option>
                <option value="rezept">Rezeptfrage</option>
                <option value="sonstiges">Sonstiges</option>
              </select>
            </div>
            <div class="form-group full">
              <label for="nachricht">Ihre Nachricht *</label>
              <textarea id="nachricht" name="nachricht" rows="5" required></textarea>
            </div>
            <div class="full">
              <button type="submit" class="btn-primary">Nachricht senden</button>
            </div>
          </form>
        </div>
      </div>

      <div style="width:100%;height:300px;border-radius:3px;overflow:hidden;margin-top:36px;">
        <iframe
          src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2594.8!2d9.1597!3d49.8019!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x47a2a4c4b4b4b4b4%3A0x0!2sPfarrer-Adam-Haus-Str.+5a%2C+63939+W%C3%B6rth+am+Main!5e0!3m2!1sde!2sde!4v1"
          width="100%"
          height="300"
          style="border:0;"
          allowfullscreen=""
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          title="Praxis Med Vital — Pfarrer-Adam-Haus-Str. 5a, 63939 Wörth am Main">
        </iframe>
      </div>
    </div>
  </section>
</Layout>
